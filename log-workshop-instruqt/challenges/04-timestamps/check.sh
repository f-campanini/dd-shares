#!/bin/bash
# Challenge 4 check - Pipeline creation is in Datadog UI, so we just verify logs are still flowing
# In a real workshop, you might use the Datadog API to verify pipeline existence

echo "Pipeline configuration is done in the Datadog UI."
echo "Verify manually that timestamps are being correctly parsed."
echo ""
echo "Check passed - proceeding to next challenge."
